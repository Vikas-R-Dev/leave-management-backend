# Leave Management Backend

A simple Spring Boot backend for leave management.

## Technologies

- Java
- Spring Boot 3.x
- Spring Web
- Spring Data JPA
- MySQL

## Setup

- Create DB: `leave_db`
- Update `application.properties` with DB credentials
- Run app: `GET /hello` returns "Welcome Vikas!"
